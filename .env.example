# Локально: скопируйте в .env.local и подставьте значения из Supabase → Project Settings → API
#
# GitHub Pages: Settings → Secrets and variables → Actions — те же имена.
# После изменения секретов запустите workflow заново (Actions → Deploy to GitHub Pages → Run workflow).
#
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key
